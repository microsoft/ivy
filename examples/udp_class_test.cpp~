#include "udp_test.h"

class myclass : udp_test {
    virtual void imp__foo__recv(int dst, int v) {
        std::cout << "process " << dst << " received " << v << std::endl;
    }
}


int main(int argc, const char **argv) {
    myclass obj;
    obj.ext_foo_send(0,1,1);
}
