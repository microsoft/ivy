#lang ivy1.6

object clock = {
    type t
    relation (X:t < Y:t)

    axiom X:t < Y & Y < Z -> X < Y
    axiom ~(X:t < Y & Y < X)

    action incr(in:t) returns (out:t)

    object spec = {
	after incr {
	    assert in < out
	}
    }
}

