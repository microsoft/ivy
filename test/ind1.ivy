#lang ivy1.7

type t
interpret t -> int

function sum(X:t) : t

definition [def_sum] {
    sum(X:t) = 0 if X <= 0 else (X + sum(X-1))
}
proof
    apply rec[t]

property sum(X) >= X
proof
    apply ind[t];
    assume sum;
    defergoal;
    assume sum;
    showgoals
    
